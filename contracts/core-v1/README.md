# Core v1 contracts

JSON Schema files in this directory are the executable semantic authority. Generated TypeScript and Zod artifacts live under `src/features/messages/contracts/generated`. Markdown documents explain decisions but never override these schemas.
